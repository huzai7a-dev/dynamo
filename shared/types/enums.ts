export enum OrderStatus {
    PENDING = 'pending',
    IN_PROGRESS = 'in_progress',
    REJECTED = 'rejected',
    DELIVERED = 'delivered',
    CANCELLED = 'cancelled',
  }
  
  export enum PaymentStatus {
    PAID = 'paid',
    UNPAID = 'unpaid'
  }
  